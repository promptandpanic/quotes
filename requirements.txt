# Core
Pillow>=10.3.0
requests>=2.31.0
python-dotenv>=1.0.0

# Google Gemini / Imagen
google-genai>=0.3.0

# Utilities
pyyaml>=6.0

# TTS narration — edge-tts is the free fallback (Microsoft neural voices)
# ElevenLabs uses requests (already listed above) — no extra package needed
edge-tts>=6.0.0
